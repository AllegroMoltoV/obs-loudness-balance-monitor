# v0.1.2 の公開前ビルド検証

検証日: 2026-09-20

## 結果

作業ブランチ `issue/1-2-validation` のコミット `0efa9c09f18af53d67afe70e7abb3076eed6b301` を対象に [GitHub Actions #35479646035](https://github.com/AllegroMoltoV/obs-loudness-balance-monitor/actions/runs/35479646035)を実行した。事前確認、macOS、Windows、Ubuntu の全ジョブが成功した。Windows 用の `loudness-balance-monitor-0.1.2-windows-x64.zip` を取得し、DLL の版情報と ZIP の内部構造を確認した。この ZIP は v0.1.2 の公開候補として使用できる。

`0efa9c0` と実機検証済みコード `a1e35ad` の間で変わったのは、版番号と文書だけである。したがって[隔離版 OBS 32.1.2 の実機検証](2026-09-20-obs-32-audio.md)は同じ音声処理コードを対象にしている。ただし、今回生成した v0.1.2 DLL 自体を OBS で再度読み込ませる検証はしていない。macOS 実機での OBS 読み込みと音声動作も未確認である。

## CI と成果物

ワークフローは `dispatch.yaml` に `job=build` を指定して起動した。実行結果の `headSha` は `0efa9c09f18af53d67afe70e7abb3076eed6b301`、`status` は `completed`、`conclusion` は `success` だった。全ジョブの成功は `gh run view 35479646035 --json headSha,status,conclusion,jobs` で照合した。監視の標準出力と標準エラーは `.logs/v012-build-35479646035-2026-09-20.log` に保存し、その SHA-256 は `F36EE8E8F8A31CAFE2A751279EF92AE1ACC22C6B5D0EC343FF19DD4E4DA156BF` である。

Windows アーティファクト名は `loudness-balance-monitor-0.1.2-windows-x64-0efa9c09f`。対象実行から `.tmp/v012-build-35479646035` へ取得した ZIP は 876,385 バイトで、SHA-256 は `34F812DC2FF38EEED6C5A4BF755850D468D854270A481758DEBE3D4CBC682B31` だった。DLL `loudness-balance-monitor/bin/64bit/loudness-balance-monitor.dll` のファイル版と製品版はいずれも `0.1.2`、SHA-256 は `A1558212E3E5EF294C0ED6F50ED8916C87E19ABE82FBE02FAFDF14474320B666` だった。

ZIP 内には次のファイルがある。

```text
include/ebur128.h
lib/ebur128.lib
lib/pkgconfig/libebur128.pc
loudness-balance-monitor/bin/64bit/loudness-balance-monitor.dll
loudness-balance-monitor/bin/64bit/loudness-balance-monitor.pdb
loudness-balance-monitor/data/locale/en-US.ini
loudness-balance-monitor/data/locale/ja-JP.ini
```

README の通常版向け手順は `loudness-balance-monitor` フォルダーだけをコピーする。ポータブル版 OBS 32.1.2 向け手順は DLL と言語ファイルを確認済みの組込探索先へ分けてコピーする。`include` と `lib` はどちらの手順でも使わない。

## 公開時の確認

現時点では `v0.1.2` タグ、GitHub Release、OBS Forum の更新は作成していない。配布物の URL と公開内容は、実際の Release を作成した後に照合する。issue #1・#2 への返信文は利用者向けの文案にとどめる。ドックの表示状態の復元は [issue #3](https://github.com/AllegroMoltoV/obs-loudness-balance-monitor/issues/3)に登録済みで、この版の動作は毎回手動表示である。
